export interface Address {
    id: number,
    googleApiId: string,
    streetName: string,
    streetNumber: string,
    city: string,
    country: string,
    postalCode: string,
    latitude: number,
    longitude: number
}